# AlongGPX Docker Configuration
# Copy this file to docker/.env for production deployment
# These environment variables configure the Flask backend container

# Flask Settings
FLASK_ENV=production
FLASK_PORT=5000

# Project Settings
ALONGGPX_PROJECT_NAME=DockerProject
ALONGGPX_OUTPUT_PATH=/app/data/output
ALONGGPX_TIMEZONE=UTC

# Search Parameters
ALONGGPX_RADIUS_KM=5
ALONGGPX_STEP_KM=
ALONGGPX_SEARCH_INCLUDE=
ALONGGPX_SEARCH_EXCLUDE=

# Overpass API Settings
ALONGGPX_OVERPASS_RETRIES=5
ALONGGPX_BATCH_KM=50
# Overpass API Servers (semicolon-separated)
ALONGGPX_OVERPASS_SERVERS=https://overpass.private.coffee/api/interpreter;https://overpass-api.de/api/interpreter;https://lz4.overpass-api.de/api/interpreter

# Cleanup Settings (web backend job registry and temp files)
ALONGGPX_CLEANUP_INTERVAL_SECONDS=600
ALONGGPX_JOB_TTL_SECONDS=21600
ALONGGPX_TEMP_FILE_MAX_AGE_SECONDS=3600
ALONGGPX_OUTPUT_RETENTION_DAYS=10

# Map Settings (for downloadable Folium maps)
ALONGGPX_TRACK_COLOR=blue
ALONGGPX_DEFAULT_MARKER_COLOR=gray
# Marker colors by filter rank (semicolon-separated)
ALONGGPX_MARKER_COLOR_PALETTE=orange;purple;green;blue;darkred;darkblue;darkgreen;cadetblue;pink

# Presets File (absolute path in container)
ALONGGPX_PRESETS_FILE=/app/data/presets.yaml
