# AlongGPX CLI Configuration
# Copy this file to cli/.env and customize for your environment
# All settings can be overridden by CLI arguments
# Paths are relative to the repository root (parent of cli/ directory)

# Project Settings
ALONGGPX_PROJECT_NAME=MyProject
ALONGGPX_OUTPUT_PATH=data/output
ALONGGPX_TIMEZONE=Europe/Berlin

# Input (can also be set via --gpx-file argument)
ALONGGPX_GPX_FILE=data/input/example.gpx

# Search Parameters
# Search radius in kilometers around track points
ALONGGPX_RADIUS_KM=5
# Distance between query points (auto-calculated as 60% of radius if not set)
ALONGGPX_STEP_KM=

# Overpass API Settings
ALONGGPX_OVERPASS_RETRIES=5
# Distance in km of track per API call (30-60km recommended)
ALONGGPX_BATCH_KM=50

# Overpass API Servers (semicolon-separated)
ALONGGPX_OVERPASS_SERVERS=https://overpass.private.coffee/api/interpreter;https://overpass-api.de/api/interpreter;https://lz4.overpass-api.de/api/interpreter

# Search Filters (semicolon-separated)
# Include: OSM tags to search for (e.g., tourism=camp_site)
ALONGGPX_SEARCH_INCLUDE=
# Exclude: OSM tags to remove from results (e.g., tents=no)
ALONGGPX_SEARCH_EXCLUDE=

# Map Settings (for Folium export)
ALONGGPX_TRACK_COLOR=blue
ALONGGPX_DEFAULT_MARKER_COLOR=gray
# Marker colors by filter rank (semicolon-separated)
ALONGGPX_MARKER_COLOR_PALETTE=orange;purple;green;blue;darkred;darkblue;darkgreen;cadetblue;pink

# Presets File
ALONGGPX_PRESETS_FILE=data/presets.yaml
