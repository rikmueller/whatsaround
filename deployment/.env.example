# AlongGPX Docker Configuration
# Copy this file to docker/.env for production deployment
# 
# NOTE: Fixed Docker configuration (FLASK_PORT, ALONGGPX_OUTPUT_PATH, 
# ALONGGPX_PRESETS_FILE) is set in docker-compose.yml.
# Advanced settings (Overpass API, cleanup timings, map colors) use defaults 
# from backend/app.py and can be overridden if needed.

# Project Settings
ALONGGPX_PROJECT_NAME=DockerProject
ALONGGPX_TIMEZONE=UTC

# Search Parameters
ALONGGPX_RADIUS_KM=5

# Presets (semicolon-separated list of preset names from presets.yaml)
# Example: shelters;drinking_water
ALONGGPX_PRESETS=

# Search Filters (semicolon-separated)
ALONGGPX_SEARCH_INCLUDE=
ALONGGPX_SEARCH_EXCLUDE=
