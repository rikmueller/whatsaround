# AlongGPX Frontend Configuration
# Copy this file to web/.env for local development
# These settings configure Vite dev server behavior

# Hostname for Vite HMR (Hot Module Replacement) and allowedHosts
# Set this to your Docker host or external hostname for remote development
# Default: localhost
ALONGGPX_HOSTNAME=localhost

# Alternative variable names (for compatibility)
# VITE_HMR_HOST=localhost
# VITE_ALLOWED_HOSTS=localhost

# API base URL (currently unused - hardcoded to /api in api.ts)
# VITE_API_BASE_URL=http://localhost:5000
