# AlongGPX Environment Configuration
# Copy this file to .env and customize the values

# Project name used for output files (xlsx and html)
ALONGGPX_PROJECT_NAME=MyProject

# Path to the GPX file to process (uncomment if you want a preset path here)
# ALONGGPX_GPX_FILE=./data/input/track.gpx

# Search radius around track points (kilometers)
ALONGGPX_RADIUS_KM=5

# OSM tags to search for (semicolon-separated, e.g., "tourism=camp_site;amenity=shelter")
ALONGGPX_SEARCH_INCLUDE=

# OSM tags to exclude from results (semicolon-separated, e.g., "tents=no;access=private")
ALONGGPX_SEARCH_EXCLUDE=

# Initial zoom level for Folium map (1-18)
ALONGGPX_MAP_ZOOM_START=10

# Timezone for file timestamps (e.g., "Europe/Berlin", "UTC", "America/New_York")
ALONGGPX_TIMEZONE=Europe/Berlin

# Hostname
ALONGGPX_HOSTNAME=localhost
