# AlongGPX Docker Production Configuration
# Copy this file to .env for Docker production deployment
#
# Advanced settings (Overpass API, cleanup timings, map colors) are
# hardcoded in docker-compose.yml with sensible defaults.

# Project Settings
ALONGGPX_PROJECT_NAME=MyProject
ALONGGPX_TIMEZONE=UTC

# Search Parameters
ALONGGPX_RADIUS_KM=5

# Presets (semicolon-separated list of preset names from presets.yaml)
# Example: shelters;drinking_water
ALONGGPX_PRESETS=

# Search Filters (semicolon-separated)
ALONGGPX_SEARCH_INCLUDE=
ALONGGPX_SEARCH_EXCLUDE=

# Hostname (only needed for remote access or custom domains)
# Example: alonggpx.myserver.com
ALONGGPX_HOSTNAME=
