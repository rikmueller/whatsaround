# WhatsAround Docker Production Configuration
# Copy this file to .env for Docker production deployment
#
# Advanced settings (Overpass API, cleanup timings, map colors) are
# hardcoded in docker-compose.yml with sensible defaults.

# Project Settings
WA_PROJECT_NAME=MyProject
WA_TIMEZONE=UTC

# Search Parameters
WA_RADIUS_KM=5

# Presets (semicolon-separated list of preset names from presets.yaml)
# Example: shelters;drinking_water
WA_PRESETS=

# Search Filters (semicolon-separated)
WA_SEARCH_INCLUDE=
WA_SEARCH_EXCLUDE=

# Seline tracking (optional)
# Enable to inject Seline tracking script in the frontend.
WA_SELINE_ENABLED=false
WA_SELINE_TOKEN=

# Hostname is set in nginx configuration
